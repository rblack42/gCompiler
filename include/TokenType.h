#pragma once

enum TokenType {
    tok_IDENT,
    tok_NUMBER,
    tok_IF,
    tok_WHILE,
    tok_INT,
    tok_INCLUDE,
    tok_SEMICOLON,
    tok_DQUOTE,
    tok_DOT,
    tok_COMMA,
    tok_SHARP,
    tok_LANGLE,
    tok_RANGLE,
    tok_LBRACE,
    tok_RBRACE,
    tok_LPAREN,
    tok_RPAREN,
    tok_SYMBOL,
    tok_EOF,
    tok_UNKNOWN,
};

